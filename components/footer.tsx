export const Footer = () => {
  return (
    <div className="absolute bottom-[calc(var(--inset)+0.8rem)] md:bottom-[calc(var(--inset)+1.5rem)] left-1/2 -translate-x-1/2">
      {/* Footer content removed - logos moved to newsletter component */}
    </div>
  );
};
